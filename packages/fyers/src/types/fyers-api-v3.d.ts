/**
 * The official SDK ships no types. Only the data socket is used, and only
 * through the narrow surface `sdk-transport.ts` declares — this stub exists
 * so the dynamic import typechecks; the real shape is asserted there.
 */
declare module 'fyers-api-v3';
