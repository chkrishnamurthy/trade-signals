/**
 * Fyers API v3 adapter.
 *
 * Empty until the market-data client lands. Every Fyers-shaped type, every
 * symbol-format quirk and every Zod schema that parses a Fyers response stays
 * behind this boundary — nothing Fyers-specific leaks into `core`, `db` or the
 * apps (CLAUDE.md, "Do not").
 */
export {};
