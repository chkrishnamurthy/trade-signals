# Operational settings for the single intraday strategy (ORB-VC). Strategy
# thresholds live in packages/core/src/intraday/config.ts and are versioned;
# these settings are hashed into the same strategy_versions row.
#
# Signals only. Simulated money is per user (paper_portfolios, ₹2,00,000 each;
# docs/planning/paper-trading-plan.md); the worker never contacts a broker
# execution API (CLAUDE.md).
enabled: true
universe: nifty50
historyDays: 14
historyConcurrency: 4
strategyRevision: 1
