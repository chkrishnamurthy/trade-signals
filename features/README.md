# Features

Feature definitions live here as Markdown files, one per feature. The tracker
discovers them automatically — there is no list to maintain. Add a `.md` file
with YAML frontmatter (`title`, `status`, `area`, optional `epic`) and it appears
in the Features tab.

Organise into subfolders as the set grows (for example `features/watchlists/`,
`features/screener/`).
