// Empty stub so Vitest can import modules that guard themselves with
// `import "server-only"`. The real package throws outside Next's server
// bundler; under test it is a harmless no-op.
export {};
