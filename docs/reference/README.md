# Reference

Look-up material — configuration options, the database schema, environment
variables, and API surfaces. Reference docs describe *what exists*, precisely,
rather than how to accomplish a task.

| Document | What it covers |
| --- | --- |
| [market-indices-strip.md](market-indices-strip.md) | The index cards under every page header — config, server cache, the two `/api/market/indices` routes, the client hook, components, and how to change it |

Add `.md` files here as other surfaces stabilise (for example
`environment-variables.md`, `database-schema.md`, `config-yaml.md`).
